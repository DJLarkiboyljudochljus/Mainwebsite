<form class="err-container" action="/contact/error" method="post">
  <h1><%= __("error-title") %></h1>
  <p><%= __("error-desc") %>: <span id="err"><%= err %></span></p>
  <input type="hidden" name="err" id="err-input" value="<%= err %>" />
  <p id="notice-prompt">
    <%= __("error-notice-prompt") %><button type="submit" class="a">
      <%= __("here") %>
    </button>
  </p>
  <button type="button" id="go-back-btn"><%= __("error-btn-back") %></button>
</form>

<script nonce="<%= nonce %>">
  document.getElementById("go-back-btn").addEventListener("click", () => {
    window.history.back();
  });

  function decodeHTML(html) {
    const doc = new DOMParser().parseFromString(html, "text/html");
    return doc.documentElement.textContent;
  }

  document.querySelector("#err").textContent = decodeHTML(
    document.querySelector("#err").textContent,
  );
</script>
